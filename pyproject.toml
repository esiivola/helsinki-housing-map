[project]
name = "helsinki-housing-map"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["geopandas", "networkx==3.6.1", "osmium==4.3.1", "PyYAML==6.0.3", "pyarrow", "scipy==1.18.1", "shapely==2.1.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
